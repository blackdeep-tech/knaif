---
title: Fine-tuning
description: When it is worth it, the union dataset, and promoting a model.
---

:::caution[Scaffold]
Track C — plan §5. Written before launch.
:::

A measured lever, not a ritual. Writing `train.jsonl`, why one shared model means every
*other* skill's snapshot is the regression gate, and the train → merge → quantize →
promote loop. The kept negative results belong here too.

Source material: `docs/FINE_TUNING.md`, `docs/TRAINING_DATA_GENERATION.md`, `docs/MODELS.md`.
