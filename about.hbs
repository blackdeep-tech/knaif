knaif — Third-Party Rust Dependency Licenses
============================================

The following open-source Rust crates are compiled into knaif. Each license text below
applies to the crates listed beneath it. Generated by `cargo about` (`just licenses`).

{{#each licenses}}
================================================================================
{{{name}}}  ({{id}})
Applies to:
{{#each used_by}}  - {{crate.name}} {{crate.version}}
{{/each}}--------------------------------------------------------------------------------
{{{text}}}

{{/each}}
