# knaif 1.2.0 — one release for the code, the retrained models and the packages

**Status:** Active · **Created:** 2026-09-25 · **Completed:** —
**Owner:** release · **Ref:** branch `release/1.2.0`; replaces the "publish v2 now" order in
TODO Open / Next; builds on [skill-quality-lifecycle](2026-09-10-skill-quality-lifecycle.md),
[inference-config-parity](2026-09-23-inference-config-parity.md),
[chain-source-threading](2026-09-23-chain-source-threading.md)

> **Status note:** decisions R0 taken 2026-09-25 with the owner, including the merge policy (repo
> settings changed and verified the same day). The `sft-v4` candidate is **not**
> published: the next 4B and 1.7B from one training cycle become the public `v2` models, released
> together with knaif 1.2.0. R1 (branch consolidation) done 2026-09-25; R2 is next.
>
> **Amended 2026-09-25 after a static audit of this plan.** It found six gaps, all confirmed against
> the code, plus two more found while checking them:
> - release metadata was edited after acceptance, which marks the evidence stale;
> - the gate holds one bar, one baseline and one L4 record per skill, not a matrix;
> - the CUDA payload publication was missing;
> - no artifact-level or OS coverage was defined;
> - wrong version files, and missing deliverables;
> - no common grading baseline for the promotion comparisons;
> - found while checking: three fingerprint keys are never computed;
> - found while checking: released-artifact records are not implemented.
>
> The fixes: a **release-candidate freeze** (R5b) comes before acceptance, and acceptance runs on
> the frozen, packaged tree; the gate tooling moves into R2; R3a measures reference runs for every
> model the release is compared against.

**Goal:** Ship knaif 1.2.0 and the retrained 4B and 1.7B models as one release, merged to `main`
from a single release branch, with every shipped path — CUDA, Vulkan and the CPU fallback —
accepted on the binary.

## Why the order changed

Until 2026-09-25 the plan was to publish `sft-v4` as `knaif-qwen3-4b-v2` on its own. The owner
revised it:

- **No partial models on HF.** A public model number is only used when something is published, so
  skipping `sft-v4` leaves no gap: the next published 4B is still `-v2`.
- **Models and knaif go together.** A model is published inside a knaif release, never alone.
- **There is no 1.7B v2**, and the published 1.7B v1 fails the safety gate (10/11,
  `ffmpeg_safety_system_root_dir`). A release that still recommends it would ship a known safety
  failure.
- **CPU is the main fallback, not an option.** The release artifact is the `vulkan` kind, which
  carries the same llama.cpp CPU backend and uses it on any machine without a usable GPU
  (NATIVE.md §5.3). The 2026-09-25 cross-backend check
  ([report](../../evals/runs/2026-09-25_backend-parity-v2_plans/report.md)) left CPU ffmpeg over
  its flip bound (37/851 decisions vs CUDA), so that path has to be accepted too.
- **Try training before accepting the backend gap.** The same ~5% of utterances flip under every
  perturbation (24 of the 35 compute-config flips are among the 44 backend flips): the model is on a
  near-tie there, and training can widen it. llama.cpp does not make backends agree bit-for-bit
  and has no setting that does (maintainer: "there isn't currently a way").

## R0 — Decisions (2026-09-25)

- [x] **knaif version: 1.2.0**, not 2.0.0. Measured against the `v1.1.0` tag: no CLI subcommand
      or flag removed or renamed (one additive flag: `backend list --json`), no change to the Python SDK's
      public API, the model manifest only gains an entry, and `knaif-qwen3-4b-v1` still runs
      correctly under the current code (ffmpeg 0.9189 in the 2026-09-24 v2 verdict). The changes are
      behavioural and belong in the release notes:
      - out-of-scope requests now `clarify` ("this skill can't do that") instead of `reject`;
        `reject` is reserved for safety-policy violations;
      - every step of a multi-step plan runs (1.1.0 ran only the first);
      - a derived output name never overwrites an existing file; edits keep the input's container;
      - a CUDA payload installed for 1.1.0 is refused by 1.2.0 and must be re-installed. This is by
        design (`knaif_version` in the backend manifest) and has been true of every release.
- [x] **Model numbers stay their own line: `knaif-qwen3-4b-v2`, `knaif-qwen3-1.7b-v2`**, not tied
      to knaif's number and not `v1.1`. Reasons:
      - knaif 1.2.1 or a macOS follow-up would ship the same models, and a model-per-knaif-major rule
        would force knaif 3.0 for the next model even with nothing broken;
      - the manifest's rule is +1 per publication, and a minor number would promise "same behaviour,
        fixes only", which a retrained model cannot;
      - the store keys on the model's filename, which is what makes a knaif upgrade with an unchanged
        model re-download nothing.

      The binding is made **explicit** instead: a `released_with:` field per manifest entry, the
      release notes, and a knaif ↔ model table on the HF card (R6).
- [x] **The 1.7B is retrained in the same cycle**, on its own bar. Safety stays at 100%. The
      quality floors may be lower than the 4B's, and they are written into the skills' acceptance
      bars **before** the model is measured.
- [x] **A 1.7B v2 ships in 1.2.0, without exception** (owner, 2026-09-25). The 1.7B is already
      released (`recommendations.mobile: knaif-qwen3-1.7b-v1`) and mobile clients depend on it, so
      "ship the 4B alone" is not an option. The v1 1.7B stays on HF; clients that pinned it keep it.

      What the evidence says it takes (`evals/runs/2026-09-16_1.7b-v4-pair_success`):
      - v1's safety failure is a **clarify where a reject is required**
        (`ffmpeg_safety_system_root_dir`), with **0 breaches**: no path emitted, nothing ran;
      - the 1.7B `sft-v4` retrain already reached **ffmpeg safety 11/11** and documents ACCEPTED
        (0.9878). Its remaining gap was ffmpeg quality against the 4B's bar (0.8954);
      - a 1.7B LoRA trains in about 9 minutes, so a 1.7B-only iteration is cheap.

      Hence the loop for the 1.7B, in this order, until it passes:
      1. **Safety below 100% → retrain, never lower.** Add paraphrased training rows for the failing
         safety intents (never the safety utterances themselves) and retrain the 1.7B alone. Its
         manifest `training_run` records the dataset it was trained on, which may then differ from
         the 4B's.
      2. **Quality below its written floor → retrain once the same way**, targeting the failing
         slices.
      3. **Still below → lower the quality floor** under the guardrails below.

      **Scope: desktop only** (owner, 2026-09-25). In 1.2.0 the 1.7B is accepted on the desktop
      matrix (R0 coverage table), not on mobile hardware. It is measured on mobile once knaif runs
      there. If it falls short, the 1.7B is fine-tuned again and published as `v3`, with its own knaif
      release per the model-binding rule.
- [x] **If the 1.7B misses its written quality bar, the bar may be lowered after measuring**
      (owner, 2026-09-25). This relaxes the rule above for the 1.7B's quality floors only, so it
      carries guardrails:
      - **safety is never lowered**: the loop above retrains until it holds on every path;
      - **the 4B's bar is never lowered this way**;
      - the floors never drop below the published `knaif-qwen3-1.7b-v1` measured under the same
        grader (R3a), since a v2 worse than v1 is not a release;
      - the revision is its own commit, recording the written floor, the measured score, the new
        floor and the reason per metric, **before** any further run is graded against it;
      - the lowered bar is stated in the release notes and on the HF card, next to the 4B's.
- [x] **The 4-hour CPU L4 runs on the release candidate, not on `sft-v4`.** `sft-v4` will not ship, so
      its CPU score would decide nothing. The "before" baseline for "did training close the backend
      gap" is the plan-level flip count already measured (CUDA vs CPU 37, vs Vulkan 29 on ffmpeg).
- [x] **macOS is not a blocker.** `contracts/release/platforms.yaml` already lists `macos` as
      `status: planned`. If it misses 1.2.0 it ships in 1.2.x, with the same models, and only the
      binary is postponed. Shared-code fixes from `feat/macos-support` (the OpenSSL guard, the
      llama-cpp-2 `openmp` feature) should still reach `release/1.2.0` if they are ready, since they
      change what every platform builds.

- [x] **L3's bar: zero port bugs, plus bounded plan disagreement** (owner, 2026-09-25). The 1.0 rate
      bar is unreachable by construction: the runtimes link different llama.cpp builds, and even
      three native backends do not agree 100%. L3 now fails on any utterance where both runtimes chose
      the same plan but rendered different commands (a porting defect, must be 0). Plan-level
      disagreement must stay within a bound written before the run, as in the 2026-09-25 backend check.
      Closes the open owner decision in [skill-quality-lifecycle](2026-09-10-skill-quality-lifecycle.md)
      (L3a) and unblocks `supported`, the only release-eligible status.
- [x] **PDFium is bundled in 1.2.0** (owner, 2026-09-25): 6.92 MB beside the binary in every
      artifact, per the 2026-07-05 decision. The download-on-demand tier of
      [skill-payloads](2026-09-21-skill-payloads.md) stays a later plan.
- [x] **Daemon and prompt-cache reuse move to 1.3.0** (owner, 2026-09-25). Reusing the cache between
      requests is what config parity switched off, because results depended on the previous request;
      bringing it back would invalidate the measurements this release rests on.
- [x] **Documents grading is strengthened before training** (owner, 2026-09-25). Fix it, re-measure
      `sft-v4` with it as the new baseline, then train (R3a).
- [x] **Coverage per OS** (owner, 2026-09-25). Everything runs on the 5080 box: Windows natively,
      Linux under WSL2 Ubuntu 24.04 (owner installs it; the box so far has only `docker-desktop`). One
      GPU for both OSes means an OS difference cannot hide behind a hardware difference. WSL exposes
      no NVIDIA Vulkan driver, so a WSL "Vulkan" run is silently a CPU run (PERFORMANCE.md §6), and
      Linux Vulkan on a GPU cannot be measured on the hardware available.

      | OS · backend | Machine | Coverage |
      |---|---|---|
      | Windows x64 · CUDA payload | 5080 | **full**: L4 + safety, `accept-native` verdict |
      | Windows x64 · Vulkan | 5080 | **full** |
      | Windows x64 · CPU | 5080 (`vulkan` artifact, GPU hidden) | **full** |
      | Linux x64 · CUDA payload | 5080-WSL | **full** |
      | Linux x64 · CPU | 5080-WSL (`vulkan` artifact, no GPU in WSL's Vulkan) | **full** |
      | Linux x64 · Vulkan GPU | — | **not measured**; stated in the release notes |

      Each model runs every full entry, so there are two CPU runs of ~4 h per model. **Runs are
      sequential, never Windows and Linux at once:** concurrent runs skew the timings, and the CPU
      already sits at 80–86 °C with one. Every run is capped at 8 threads.

      Clean-room checks (both OSes) and the Windows upgrade check (RELEASE.md §4) run on the packaged
      artifacts in every case.

## - [x] R1 — One release branch

`release/1.2.0` was created from `feat/skill-prompt-workbench`, which already contains
`skill-quality-lifecycle`, `taxonomy-retrain-and-v2-promotion` and `per-backend-build-profiles`.

- [x] Branch created and the open working-tree changes committed (the Vulkan nudge measurement,
      this plan, the workbench notebook, the merge-policy docs).
- [x] Bring across the two plan docs that exist only on `bugfix/ffmpeg-audio-format`:
      `2026-09-08-model-output-improvement.md` and `2026-09-08-terse-no-audio-modifier.md` (indexed,
      and the dead link to the folded 2026-08-08 parity plan repointed).
- [x] Verified by measuring, per commit, how many of its added lines exist in the release branch:
      - `audit/codex-17-09-26`: 99–100% present. Safe to delete.
      - `origin/feat/native-parity` (24 commits, 2026-08-08..11): **0–12% present. NOT superseded
        as a whole.** Its parity-plan docs and eval runs were deliberately replaced by the lifecycle
        process (`e279aae`, `f33579b`), but its code is missing: the native port of the second
        chain-linking pass (forward-threading, `73c6e9e`), and cross-runtime contracts for
        expansion (8 ffmpeg chain shapes plus documents) and for chain linking. Today's L2
        `planner_cases.json` has 23 validation-only cases and no multi-step plan. Keep the branch;
        port selectively in R2. Do not merge it whole: it is six weeks behind and would conflict
        with everything that replaced its plan.
- [x] Delete the merged or superseded branches (owner runs the commands):
      - `audit/f6-regression-fail-open` and `bugfix/ffmpeg-audio-format` (merged as #56);
      - `pr57` (merged as #57);
      - `integrate/2026-09-17-audit-fixes` (all 6 commits already here);
      - `audit/codex-17-09-26` (verified present).
- [x] Leave out `feat/ai-skill-direct-calls` and `feat/local-site-analytics`. Each is one plan
      commit for future work; rebase them onto `main` after the release.
- [x] Push `release/1.2.0` (owner). The macOS developer merges it into `feat/macos-support` now, and
      again whenever it moves.
- [x] From here on, work reaches `release/1.2.0` only through short `feat/*` or `fix/*` branches
      and PRs.
- [x] **Merge policy (owner, 2026-09-25).** Release and big feature branches merge into `main` with a
      **merge commit**; short one-off fixes may still squash. Reason: other people build on these
      branches (the macOS developer merges `release/1.2.0` into `feat/macos-support`), and a squash
      creates new commits git cannot match to the originals, so their later merge conflicts and
      duplicates history. It also keeps every commit an eval report cites reachable from `main`. Read
      `main` one entry per PR with `git log --first-parent main`. A release branch takes in `main`'s
      changes by merging, never rebasing. Repo settings to change (owner): in the `main-guardrails`
      ruleset untick **Require linear history** and add **Merge** to the allowed merge methods; in
      Settings → General tick **Allow merge commits**. Everything else in the ruleset stays.
      *Done 2026-09-25, verified through the API; CONTRIBUTING.md now describes both methods.*
- [x] **CI lints every commit of a PR, not only its title.** With merge commits, an integration
      branch's commits land on `main` unchanged, and until now only the local `commit-msg` hook
      (opt-in) checked them. `scripts/check_commit_msg.py --range BASE..HEAD` lints every non-merge
      commit and names each failing one; the `pr-title` job runs it with full history over the PR's
      base and head SHAs (passed through `env`, never interpolated). Tests:
      `test_check_commit_msg.py`. All 160 non-merge commits of `origin/main..release/1.2.0` pass.

## - [ ] R2 — Finish the code

- [x] [Chain source threading](2026-09-23-chain-source-threading.md): the named-once rule, never
      thread onto a file of a different kind, the native port (T4). **Start T4 from
      `origin/feat/native-parity` `73c6e9e`**, which already ported the forward-threading pass to
      native on 2026-08-10 and never merged, adapted to the new rule.
      *Done 2026-09-26 on `fix/chain-source-threading`: the named-once rule (ASCII boundaries,
      so unspaced CJK counts), the kind rule via skill-declared `file_kinds:` (owner decision), the
      native port, 17 L2 cases, and fan-out rows `ffmpeg_301/302`. T6: identical plans and scores on
      every shared utterance, both skills ACCEPTED, and the ffmpeg snapshot re-locked at 861 utt.*
- [ ] **Port the cross-runtime expansion and chain-linking contracts** from `origin/feat/native-parity`
      (`expansion_cases.json`, `documents_expansion_cases.json`, `chain_linking_cases.json` and their
      Python and Rust consumers) into L2, re-deriving the golden plans under the new threading rule.
      Then delete the branch.
- [ ] A codec written as an extension (`*.hevc`) maps to a container.
- [ ] `frames` together with a zero-length range is accepted (`ffmpeg_161`).
- [ ] **Bundle PDFium in the native builds** (R0; TODO Open / Next). OCR fails without it; the evals
      only pass because they point at pypdfium2's copy. This is a release blocker. Pin the version and
      sha256, stage it in `build_native_kind.sh` and `package.sh`, add it to `NOTICE` and the
      provenance report, and make `installers/smoke.sh` assert it is present.
- [ ] **L3 gate to the new bar** (R0): the L3 check reports and gates on port bugs (same plan,
      different commands) at 0 and on plan disagreement against a pre-written bound; update
      `contracts/release/native_status.yaml`, `check-gate` and AGENTS.md's layer table.
- [ ] The cross-backend check becomes a reusable recipe (`just eval-backends <skill>` or similar)
      instead of a one-off run script: plans on CUDA, Vulkan and CPU, safety on each binary, the
      flip bounds and the fallback L4, with the thread cap and the CRLF fix carried over.
- [ ] **The gate holds an acceptance matrix, not one record per skill** (audit). Needed before R4:
      without it, R5 cannot keep what it measures.

      Today there is one of each per skill:
      - one `acceptance.yaml`;
      - one `data/eval_snapshot.json`, and `check_native_acceptance` refuses a run whose model differs
        from it;
      - one L4 entry in `evals/acceptance/<skill>.json`, which each `accept-native` overwrites
        (`gate.py` `record_layers`).

      So the last passing run hides an earlier failure, and locking the 1.7B snapshot replaces the
      4B reference.

      Build:
      - a contract, `contracts/release/acceptance_matrix.yaml`, listing each release's
        models × backends × OS entries, with each entry's coverage (full, or not measured, per R0);
      - per-model bars: 1.7B floors beside the 4B's, safety 100% for both;
      - per-model snapshots;
      - L3/L4 records keyed by model and backend;
      - `check-gate` reports `supported` only when **every** full-coverage entry is valid.

      TDD. The RED test: a passing CUDA verdict recorded after a failing CPU one still leaves the
      skill short of `supported`.
- [ ] **Fingerprint the keys the contract already names** (found while checking the audit).
      `native_status.yaml` lists `model`, `native_binary` and `policy` under L3/L4 `invalidated_by`.
      `evidence_tuple()` never computes them, so `_layer_state` skips them silently
      (`if key in current`). Swapping the GGUF or rebuilding the binary therefore leaves L4 "valid".
      Record the GGUF's sha256, the binary's sha256 and `POLICY_VERSION` from the run itself. The
      RED test: the same record with a different GGUF sha256 turns stale.
- [ ] **Release records (`gate.py`'s docstring promises them; nothing implements them).** At the
      tag, copy the matrix records to `evals/acceptance/releases/1.2.0/`. The live gate may then go
      stale on `main` while the answer to "what was true for 1.2.0" survives.
- [ ] **L4 runs the packaged layout, with no PDFium override.** The lanes and the 2026-09-25 scripts
      export `KNAIF_PDFIUM_PATH` pointing at pypdfium2's copy. That proves OCR works with a library
      the user will not have. The lane must run the binary from the unpacked artifact, with the
      variable unset. A test asserts the lane environment carries no `KNAIF_PDFIUM_PATH`.
- [x] Blackwell removed from `nudge.vulkan_inadequate_compute_caps` after re-measuring
      (PERFORMANCE.md §2, 2026-09-25).

## - [ ] R3a — Strengthen documents grading, then re-baseline

- [ ] Give the documents rows that are graded only on tool choice and file existence (87 of 132 plan
      rows) real checks of the transformation: rotation, page order and content, page counts,
      watermark text, and similar. `documents_036` (rotates page 1 of 3 and scores 1.0) is the test
      case.
- [ ] **Reference runs under the final grader** (audit). Every model R5 compares a candidate
      against is re-measured under the grader, corpus and generation config the candidates will
      face, at the quantization it ships in (Python `success`, full corpus, both skills):
      - `sft-v4` (4B Q4_K_M);
      - published `knaif-qwen3-4b-v1`;
      - published `knaif-qwen3-1.7b-v1` (Q6_K).

      Historical scores are not comparable once documents grading changes, so no promotion verdict
      quotes one. `sft-v4`'s run becomes the 4B documents baseline, with the snapshot re-locked in its
      own commit. If the grader or corpus changes again before R5, these runs are repeated.

## - [ ] R3 — Training data

- [ ] The fragile set: every utterance that flipped across backends or compute configs, from
      `evals/runs/2026-09-25_backend-parity-v2_plans/flips_*` and the config-parity T6 flip lists
      (~55 utterances). Write **new paraphrases** of those intents for `train.jsonl`, never the eval
      utterances themselves: the train/eval verbatim-copy test must stay green, and a model trained
      on its own test questions proves nothing.
- [ ] The config-parity T8 fragile-row backlog.
- [ ] The v4 rows already in `train.jsonl` that no shipped model was trained on (terse "with no
      audio" and friends).

## - [ ] R4 — One training cycle, two sizes

- [ ] Write the 1.7B acceptance bar per skill first, in the per-model bar format from R2. Safety
      100%; each quality floor stated with its reason. Committed before training starts.
- [ ] Train the 4B and the 1.7B on the union dataset (`docs/FINE_TUNING.md`), then merge and quantize
      (4B Q4_K_M, 1.7B Q6_K).
- [ ] If the 1.7B fails R5a, run the R0 loop for the 1.7B (retrain alone, ~9 min per LoRA) before
      touching its floors. The 4B does not wait for it: the 1.7B joins at the R5b freeze.

## - [ ] R5 — Evaluate, freeze, accept

**Why a freeze sits in the middle** (audit). The evidence fingerprint (`gate.py`
`evidence_tuple`) hashes `contracts/**/*.yaml` and every skill's `*.yaml`. The version bump, the
model manifest entries, the CUDA payload hashes, `recommendations:` and `recommended_model:` all
live there. If they change after L3/L4, the release ships on stale evidence, and a package smoke
test does not renew it. So every one of them is final **before** the binary acceptance runs.

**Rerun rule** (owner, 2026-09-25). If a fingerprinted file changes after the freeze (a bugfix
found during R5c), that change becomes a new freeze commit and the new release candidate. Only the
matrix entries `check-gate` then marks stale are re-run. A ~4 h CPU run repeats only if its inputs
changed. R2's fingerprint work (model, binary, policy) is what makes "stale" trustworthy here.

### R5a — Python, per size

A failure stops the chain for that model. For the 4B it blocks the release; for the 1.7B it
enters the R0 loop (retrain, then floors), and the chain restarts on the new candidate.

- [ ] Python `success` on the full corpus, S2 acceptance at that size's bar, safety 100%, regression
      against the size's snapshot.
- [ ] Promotion verdict against the R3a reference runs, never a historical score, with the decision
      rules written before the runs.
- [ ] Re-lock the per-model snapshots (own commit).

### R5b — Freeze the release candidate (one commit, then no fingerprinted edits)

- [ ] Version 1.2.0 in all four places `test_version_consistency.py` checks:
      - `Cargo.toml` `[workspace.package]`;
      - `python/core/pyproject.toml` (not the root workspace file, which carries no version);
      - `installers/windows/knaif.iss` `AppVersion`;
      - `contracts/backends/backend-manifest.yaml` `knaif_version`.
- [ ] Model manifest entries for both v2 GGUFs: `sha256` from the local quantized files; `url` at
      the HF path they will be uploaded to; `released_with: "1.2.0"`. Then move `recommendations:`
      and both skills' `recommended_model:`.

      Until R7 uploads the files, `knaif models pull` on this branch fails with a 404 (accepted by the
      owner, 2026-09-25: nothing goes public before the release). The macOS developer uses the local
      GGUF (via `models.yaml`) until then.
- [ ] **CUDA payload, RELEASE.md §7 steps 1–2a**, both OSes:
      - build `--kind=cuda` with the full `CUDAARCHS` (clean build, ~1 h on Windows);
      - `package.sh` writes the manifest fragment with real hashes and sizes;
      - `rehearse-backend-install.sh` passes;
      - the fragment is spliced into `backend-manifest.yaml`, the product files' URLs moved to
        `releases/download/v1.2.0/` (the `redist-cuda-13.3` URLs stay).

      Without this, a 1.2.0 receipt would name the v1.1.0 `ggml-cuda` bytes.
- [ ] `just licenses-all` (PDFium now bundled) and `NOTICE`.
- [ ] Build and package every artifact from this commit: Windows `vulkan` installer and zip, Linux
      tarball, both CUDA payloads. Record the commit SHA; it is **the release candidate**.

### R5c — Accept the frozen candidate, from the packaged artifacts

Every entry of the R0 coverage table, run from the unpacked or installed artifact, with
`KNAIF_PDFIUM_PATH` unset (R2).

- [ ] L3 parity at the R0 bar, per model.
- [ ] **Cross-backend check**: plans on CUDA, Vulkan and CPU. Success criterion for R3: fewer flips
      than `sft-v4`'s 37 (CPU) and 29 (Vulkan) on ffmpeg, recorded with the prediction written first.
      Fewer flips counts only together with the quality floors: two backends agreeing more often on a
      wrong plan is not an improvement. Report each flip set split into one-side-correct and
      both-wrong, graded by the `success` verifier.
- [ ] **L4, every full-coverage entry of the R0 table**, per model, each with its own
      `accept-native` verdict kept in the matrix (R2):
      - CUDA through the **installed** payload (the rehearsal's local server), not a CUDA build:
        Windows, then Linux in WSL;
      - Vulkan, from the Windows `vulkan` artifact;
      - CPU from the **same `vulkan` artifact** (on Windows with the GPU hidden:
        `CUDA_VISIBLE_DEVICES=-1` and no Vulkan device), not a separate `cpu` build. Windows, then
        Linux in WSL, ~4 h each at 8 threads.

      A failing 4B entry blocks the release; CPU cannot be dropped. A failing 1.7B entry enters
      the R0 loop: a safety failure means retraining, never a lower bar. A retrained 1.7B changes
      the model manifest, so it is a new freeze (rerun rule) and its own entries re-run.
- [ ] RELEASE.md §4 on the packaged artifacts: clean-room (both OSes), Windows upgrade over 1.1.0
      with the CLI running, installer smoke asserting PDFium is present **and** loads (an OCR row, not
      a file check).
- [ ] `check-gate` on the release-candidate commit: every full-coverage entry valid for both skills.

## - [ ] R6 — The HF model card lives in the repo

The card at https://huggingface.co/blackdeep/knaif is edited on HF only. It is stale:
- it tells users to run `knaif models pull qwen3-4b-v1` (the key has been `knaif-qwen3-4b-v1` since
  2026-07-18);
- its metadata names `Qwen/Qwen3-1.7B-Base` as the base model for the whole repo;
- its GitHub links are broken;
- it never mentions knaif.org or knaif.dev.

- [ ] Add the card to the repo (e.g. `contracts/models/HF_MODEL_CARD.md`) with correct YAML front
      matter and links to knaif.org (users), knaif.dev (developers) and the GitHub repo. Include a
      knaif ↔ model table (which knaif release ships which model files) and the eval numbers per
      model and backend.
- [ ] `scripts/publish_model.py` uploads the card with the models, so it cannot drift again.
- [ ] Optional, before the release and with owner go-ahead: fix only the v1 card's broken links and
      pull command now, since it is live and wrong today.

## - [ ] R7 — Release

Every outward-facing step needs the owner's explicit go-ahead at that step. **Nothing here edits a
fingerprinted file**: everything published is the byte-identical output of R5b, and each upload is
checked against the sha256 already in the manifest.

- [ ] Publish both GGUFs and the card to HF (`scripts/publish_model.py`). Verify the uploaded
      files' sha256 against the manifest, then `knaif models pull` both from a clean store.
- [ ] Merge `release/1.2.0` into `main` with a **merge commit** (R1 merge policy). Every eval report
      records the commit it ran on (`2149cbc`, `ec6e681`, …), and the `feat/macos-support` branch
      builds on these commits. Check `git diff --quiet <release-candidate> <merge commit>` first: if
      `main` moved meanwhile, the merge tree is not the tree that was accepted and packaged, and R5c
      is re-run for what changed.
- [ ] Tag `v1.2.0` on the merge commit. GitHub release as a draft first (RELEASE.md §5):
      - the R5b artifacts;
      - the CUDA payload product files (RELEASE.md §7, the upload half);
      - `SHA256SUMS` generated last, over the complete final set.

      Its notes carry the behavioural changes from R0, the knaif ↔ model table and the Linux Vulkan
      coverage gap. Then publish the draft, and install the CUDA payload from the live URLs once.
- [ ] Python wheel and sdist to PyPI (RELEASE.md §5a).
- [ ] Copy the matrix records to `evals/acceptance/releases/1.2.0/` (R2).
- [ ] Refresh the site release data and the published eval numbers (T9 of
      [reject-clarify-taxonomy](2026-09-11-reject-clarify-taxonomy.md)).
- [ ] Rebase `feat/ai-skill-direct-calls` and `feat/local-site-analytics` onto `main`.
